{
  "cmd": ["bash", "-c", "g++ -std=c++23 -DLOCAL '${file}' -o '${file_path}/${file_base_name}' && clang-format -i '${file}'"],
  "working_dir": "${file_path}",
  "selector": "source.c, source.c++, source.cpp, source.cc",
}